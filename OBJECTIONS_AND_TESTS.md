# Standard objections and where they are tested

Reviewers of this repository tend to raise a recurring set of objections. Several are built into the study as falsification tests; others mark declared open points. This page indexes both, with the place where each point is tested or stated. The list is not exhaustive. Rows whose test lives in this repository carry a path in backticks and can be recomputed here; rows citing only §-numbers point into the manuscript.

Reading note: the 50-page empirical manuscript is included at the repository root as `paper_en_v7_2026-08-18.pdf` (author's version, build of 18 Aug 2026); the previous states `paper_en_v6_2026-08-18.pdf` and `paper_en_v5_2026-08-12.pdf` stay byte-identical next to it. The manuscript carries the within-substrate correlation evidence, the falsification section (§8), and the substrate chapters cited below. The v6 build incorporates the first four items of `REVISION_NOTES.md`; the v7 build incorporates points 6 and 7 (timber decomposition, music control per the pre-registration).

Reference state: empirical manuscript v7 (build of 18 Aug 2026; the rows below were indexed against v5; rows 2 and 6 are updated for v7, the other rows hold unchanged) · formal framework, 30 Jul 2026 · repository snapshot policy in `README.md` (Reference state).

## Objections that are built in as tests

| # | Objection | Where it is tested | Result |
|---|---|---|---|
| 1 | Normalized cumulative monotone series fit sigmoids almost regardless of input; high R² proves nothing. | Manuscript §8.2 (Layer 1b) raises this objection itself; `data/model_comparison/`. | On random monotone curves the two-parameter power law dominates and the four-parameter sigmoid is never the winning class; on the empirical substrates the logistic wins 6 of 8 (`data/model_comparison/README.md`). The criterion is the class comparison with complexity penalized, and the inflection-outcome relation holds across all four model families (§8.2). |
| 2 | If R² is not the evidence, what is? And does the timber correlation not follow from the normalization itself, since sigma_max sits in the denominator of the fitted axis? | Manuscript §4.1 (since v7) and §4.12 reading rule: "the load-bearing statement is the within-substrate correlation, not the absolute value"; `analysis/timber_normalization_control.py` (+ output file), readings [C] and [D]. | For timber it largely does, and §4.1 states it since v7: the affine decomposition sigma_onset = 5.27 + 0.433·sigma_max (R² = 0.9925, reading [D]) makes â ≈ 0.433 + 5.27/sigma_max a nearly deterministic decreasing function of strength, so the near-perfect rank order (Spearman -0.994) follows arithmetically from the established onset-strength relation; the permutation baseline (reading [C], r ≈ -0.74 with the onset permuted independently of strength) answers only the narrower question of whether the onset is independent of strength (it is not: CV 0.23, within-class r = -0.83/-0.86/-0.95). The timber anchor therefore serves as instrument validation on known sorting physics: â recovers the onset-strength relation and orders the visually assigned DIN 4074 grades monotonically; it does not evidence a mechanism beyond that relation. The load-bearing cross-substrate evidence sits in the substrates without such a deterministic construction, e.g. LLM conversations: rho(â, annotated failure) = -0.359 cluster-robust, n = 202 (§4.10). An out-of-sample test path (strength estimation without sigma_max among the inputs) is named in `REVISION_NOTES.md` point 6. |
| 3 | AR(1)/random-walk nulls are too weak; surrogates preserving each substrate's marginals are required. | Manuscript §1.2 (demarcation), §4.3, §4.7, §4.10, §4.11, §8. | Free-ordering substrates carry such surrogates: turn-order destruction (conversations), note-order shuffle (music), a null with identical event statistics (earthquakes), length-matched calm phases (finance: R² 0.48 against 0.97 in crashes). For forced-ordering substrates (tensile test) a path surrogate is physically meaningless; there the test is the model comparison of row 1. |
| 4 | Points along a cumulative curve are autocorrelated; pooled inference overstates confidence. | Methods paper arXiv:2604.14414 (public); manuscript §3.3; `STATISTICS.md`. | The autocorrelation is treated as loading-history signal, and inference is cluster-robust with the specimen as the unit of analysis. The methods paper quantifies the overstatement of pooled inference; its title carries the 42% figure. |
| 5 | Ten substrates and many statistics invite a look-elsewhere effect. | `analysis/bonferroni_gamma_m.py`; `STATISTICS.md`; manuscript §1.4, §4.13. | Reporting is Bonferroni-conservative under the safety factor γM = α/9; the count recomputes here. The per-substrate status table separates conformant results from nominal-with-named-caveat (§4.13). |
| 6 | The music shuffle control keeps high R² and visible genre differences. | Manuscript §4.11 (since v7); pre-registration `data/musik_companion/preregistration_musik_l2.md` (provenance: `PREREGISTRATION_PROVENANCE.md` in the same folder); `data/musik_companion/outputs/musik_l2_shuffled_control.json`. | The pre-registration governs the reading. Its §5 expected â to degenerate to a tight cluster near 0.5 with the genre separation vanishing, and fixed a reduction clause: separation surviving the shuffle marks a vocabulary effect. Observed: no 0.5 collapse (â clusters at 0.27-0.34), the monotone genre ordering breaks (Mann-Kendall n.s.), and the Jazz level stays separated. Under the clause, the monotone ordering is sequence-carried and the Jazz level carries a vocabulary component; §4.11 reports the control as this mixed result since v7. High R² under shuffle is expected (row 1) and is no evidence criterion. H1 itself (genre order fixed per Di Marco before analysis) is untouched by the control: τ = +1.000 in the real run. A pipeline docstring briefly carried the manuscript wording instead; restored to the pre-registration wording 18 Aug 2026, pipeline and reported values unchanged. |
| 7 | Colorectal (CRC): the patient-level bootstrap sign opposes the stage-aggregated headline. | Manuscript §4.9; `data/crc/`. | The pre-specified sign is carried by two orderings: the stage-aggregated Δâ = -0.153 and a per-patient stage ordering with the same sign (§4.9). The bootstrap-mean comparison carries the opposite sign; the sign direction depends on the aggregation (§4.9). The "counting artifact" label there refers to a third comparison (mutation-count sorting). Survival coupling is declared open. |
| 8 | DP1180 inflection fits sit at the parameter boundary. | `data/dp1180/numisheet_results.json`; manuscript Table 6 and §8 (G8). | DP1180 serves as a reference point on the 1D axis that the manuscript itself marks as illustrative; its outcome correlation is declared underpowered (n = 19). No load-bearing claim rests on the 0.975 value. |
| 9 | V-Dem episodes are preselected and only 89 of 117 are retained. | Manuscript §4.5 and Table 1; `data/vdem/srds_vdem_results.csv`. | Episodes are defined externally (ERT-v14, Maerz et al. 2024). The count (89 of 117) is printed in Table 1. Of the 89 fits, 60 place the inflection inside the episode's observation window and 29 outside it (16 past the episode end, 13 before the episode start); the split recomputes from the bundled CSV with the criterion ep_start ≤ â ≤ ep_end (§4.5, stated since v6). |
| 10 | Is the failure quantity independent of the â fit, and how instrument-dependent is the reading? | Manuscript §3.1, §3.3, §8.5; methods paper arXiv:2604.14414. | The failure quantities are measured independently of the curve fit: fracture stress from the destructive test, a separate annotation protocol for conversations. Encoder dependence is carried as a construct-internal prediction (§8.5), with a second encoder as triangulation (§3.1); the validation load is carried by cross-corpus replication across twelve corpora (§3.3, §6). |
| 11 | The transition is assumed via L2 rather than derived from self-reference. | Formal framework, abstract and §2.4, Remarks 2.3 and 2.4. | Stated there in the same words: the reduction is "a consistency result under named postulates, not a derivation from self-reference alone". L2 is the declared load-bearing constitutive law, with its continuum-damage referent; A0 enters through the state dependence of the rate and the non-involutivity used for irreversibility. |
| 12 | T1 needs strong conditional-Gaussian assumptions; T2 is Banach plus standard ergodic theory. | Formal framework §5 and §6, Remark 5.6, Definition 6.1. | Declared: T1 is a finite-window diagnostic under named hypotheses; T2 requires the named contraction hypothesis beyond the axioms. |
| 13 | The no-go theorems are elementary. | Formal framework §9 to §11. | Declared: "elementary as analysis; its force is protective" (§9); "not new mathematics ... locative" (Remark 10.4). They fix what may not be claimed: universal constants, static verdicts, specimen-sure prospection. |
| 14 | Sorry-free Lean is not axiom-free verification. | `lean4/CHECKAXIOMS_OUTPUT.txt`; `lean4/PositivityProofs/`. | Audited: the certified theorem depends on 12 project axioms (2 numerical-certificate, 10 IFT-bridge) plus 3 Lean foundational axioms, listed per `#print axioms`; the three differing reference counts (12, 18, 27) are reconciled in the evidence file. The Arb interval certificate carries the numerical enclosure. |
| 15 | The withheld calibration thresholds make the deployed state machine non-reproducible. | `COMMITMENT.md`; `reproduce.py`; manuscript §5.6, §9.5. | Exclusively the operational thresholds are withheld, bound by a SHA-256 commitment dated 12 Jun 2026 (`COMMITMENT.md`). No reported finding depends on them; every substrate result indexed here recomputes without them (`reproduce.py`, 33 checks). |
| 16 | Only confirmations are reported. | Manuscript §8.2 (Layer 3), §6.5; `REPRODUCIBILITY_MANIFEST.md`; README (null controls). | The primary decision rule of the sealed pre-registered batch was not met (§8.2, Layer 3); the pre-registered meta-hypothesis held on both batches. Two pre-registered positive predictions were falsified (Sotopia, Google COVID Mobility) and the pre-registered null control was confirmed (ProsocialDialog); the README groups all three as pre-registered controls, the manuscript separates the two kinds (§6.5). Substrate changes are logged in the manifest: a DSC protein-melt test was withdrawn over a provenance gap, and the battery substrate uses real CALB cells (an earlier synthetic NASA-PCoE surrogate is documented there). |

## Open by declaration

Stated as open in the documents themselves and kept open here, each with its named test path:

- The empirical premise of the second no-go, that a class with self-defeating reception is non-empty; a controlled mode-switch experiment would test it (formal framework, Remark 10.4).
- The Goldilocks principle T3: interior balance is proved, the placement near criticality is an empirical prediction (formal framework, §7).
- The pathwise specimen inflection: the transition theorem is stated at the level of the expected trajectory (formal framework, Theorem 2.2).
- The general-asymmetry statement behind the no-go: one certified direction suffices for non-universality; the general case is an open conjecture (formal framework, Remark 9.6).
- The cognition bridge: a theoretical extension with a defined longitudinal falsification path (manuscript §7.1).
- CRC survival coupling (manuscript §4.9) and battery end-of-life (EOL) coupling (manuscript §4.8).
- Independent cross-group replication: two structured programs, causal intervention and multi-agent symmetry sweep (manuscript §9.2).
